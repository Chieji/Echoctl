interface:
  display_name: "Echoctl"
  short_description: "Repo-specific patterns and workflows for Echoctl"
  default_prompt: "Use the Echoctl repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true